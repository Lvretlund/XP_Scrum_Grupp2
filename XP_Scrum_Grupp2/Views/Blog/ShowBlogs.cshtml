@model XP_Scrum_Grupp2.Controllers.PostIndexViewModel

@{
    ViewBag.Title = "ShowBlogs";
}


<h2>FormalBlog</h2>
<br />
@Html.Partial("CreatePartial")


<table class="table">
    @*<tr>
    
        <th>
            @Html.DisplayNameFor(modelItem => modelItem.)
        </th>
        <th>
            @Html.DisplayNameFor(modelItem => modelItem.Text)
        </th>
        <th>
            @Html.DisplayNameFor(modelItem => modelItem.Date)
        </th>
        <th>
            @Html.DisplayNameFor(modelItem => modelItem.File)
        </th>
        <th></th>


    </tr>*@

    @foreach (var item in Model.FormalBlogs)
    {
    <tr>
        <td>
            <p>  @Html.DisplayFor(modelItem => item.Author.Firstname) @Html.DisplayFor(modelItem => item.Author.Lastname) skrev den @Html.DisplayFor(modelItem => item.Date)</p>
            <p>
                @Html.DisplayFor(modelItem => item.Text)
            </p>

            
            <a href="Download?file=@item.Filename" download="@item.Filename">@Html.DisplayFor(modelItem => item.Filename)</a>

        </td>
   

        <td></td>
    </tr>
    }
</table>

